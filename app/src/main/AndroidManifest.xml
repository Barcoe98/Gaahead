<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ie.wit">

    <application
        android:name=".main.MainApp"
        android:allowBackup="true"
        android:icon="@drawable/gaaheadlogo2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/gaaheadlogo2"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ResultActivity"></activity>
        <activity android:name=".activities.FixtureActivity" />
        <activity
            android:name=".activities.Home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".fragments.FixtureListFragment" />
    </application>

</manifest>